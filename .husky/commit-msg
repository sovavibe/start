#!/usr/bin/env sh
# Commit message validation hook
# Checks commit message format using commitlint
# Also validates ASCII-only characters

set -e

echo "🔍 Validating commit message..."

# First, check for ASCII-only characters
COMMIT_MSG_FILE="$1"
if [ -f "$COMMIT_MSG_FILE" ]; then
    # Check if commit message contains non-ASCII characters (bytes > 127)
    # Use LC_ALL=C and od to check for bytes outside ASCII range (0x00-0x7F)
    if LC_ALL=C od -An -tx1 "$COMMIT_MSG_FILE" 2>/dev/null | LC_ALL=C grep -q '[89abcdef][0-9a-f]'; then
        # Found bytes >= 0x80 (non-ASCII)
        echo "❌ Commit message contains non-ASCII characters"
        echo "💡 Use only ASCII characters (0x00-0x7F)"
        echo "💡 Non-ASCII characters (Unicode, emoji, special chars) are not allowed"
        exit 1
    fi
fi

# Check if npx is available
if ! command -v npx >/dev/null 2>&1; then
    echo "⚠️  npx not found, skipping commitlint check"
    echo "💡 Install Node.js to enable commit message validation"
    exit 0
fi

# Check if commitlint is installed locally (fastest, no network calls)
# Only use local commitlint to avoid npx hanging or network delays
COMMITLINT_CMD=""
if [ -f "node_modules/.bin/commitlint" ]; then
    # Use local commitlint (fastest, no network calls)
    COMMITLINT_CMD="node_modules/.bin/commitlint"
fi

# If commitlint is not available locally, skip validation gracefully
# Don't use npx to avoid hanging or slow network calls during commit
if [ -z "$COMMITLINT_CMD" ]; then
    echo "⚠️  commitlint not found locally, skipping check"
    echo "💡 Run 'npm install' to install commitlint"
    exit 0
fi

# Validate commit message
if ! $COMMITLINT_CMD --edit "$1" 2>&1; then
    # If commitlint fails, check if it's due to missing dependencies
    if [ -f "package.json" ] && [ ! -d "node_modules/@commitlint" ]; then
        echo "⚠️  commitlint dependencies not installed, skipping check"
        echo "💡 Run 'npm install' to install commitlint dependencies"
        exit 0
    fi
    # If it's a real validation error, fail
    echo "❌ Commit message validation failed"
    exit 1
fi

echo "✅ Commit message is valid"
