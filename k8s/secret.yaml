apiVersion: v1
kind: Secret
metadata:
  name: start-secret
  namespace: start
type: Opaque
stringData:
  MAIN_DATASOURCE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  # Add other secrets here (base64 encoded or use kubectl create secret)

