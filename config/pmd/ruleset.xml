<?xml version="1.0"?>
<ruleset name="Custom Ruleset"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        PMD ruleset for application - Optimized minimal exclusions
        Following modern Java 21+ best practices with harmonized tool configuration

        Each exclusion includes:
        - Detailed justification with code examples
        - Explanation of why exclusion is necessary
        - Alternative solutions (if applicable)
    </description>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <!-- Exclude rules that are too strict or not applicable -->
        <exclude name="GuardLogStatement"/> <!-- Modern SLF4J handles level checks efficiently -->
        <exclude name="UnusedPrivateMethod"/> <!-- Lombok may generate methods that appear unused -->
        <!-- Note: NonSerializableClass is in errorprone.xml, excluded there -->
        <exclude name="LooseCoupling"/> <!-- Framework code often requires specific implementations -->
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <!-- Exclude rules that are too strict for modern Java -->
        <exclude name="CommentDefaultAccessModifier"/> <!-- Package-private is acceptable -->
        <exclude name="LongVariable"/> <!-- Descriptive variable names are preferred -->
        <exclude name="ShortClassName"/> <!-- Entity classes like User are acceptable -->
        <exclude name="UnnecessaryAnnotationValueElement"/> <!-- Modern Java style allows value= -->
        <exclude name="UselessParentheses"/> <!-- Parentheses can improve readability -->
        <exclude name="OnlyOneReturn"/> <!-- Early returns improve code readability -->
        <exclude name="AtLeastOneConstructor"/> <!-- Default constructor is acceptable -->
        <exclude name="ShortVariable"/> <!-- Short variable names are acceptable in some contexts -->
        <!-- Note: UnnecessaryFullyQualifiedName is enabled by default - checks annotations and classes -->
    </rule>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <!-- Exclude CyclomaticComplexity to configure it separately below -->
        <exclude name="CyclomaticComplexity"/>
        <!-- Exclude rules that are too strict for framework code -->
        <exclude name="LawOfDemeter"/> <!-- Framework code often uses method chaining -->
        <exclude name="SignatureDeclareThrowsException"/> <!-- Framework methods may throw Exception -->
        <!-- Exclude AvoidCatchingGenericException for DotenvConfig -->
        <!-- Dotenv library throws various runtime exceptions (IOException wrapped, parsing errors) -->
        <!-- Catching RuntimeException is necessary as we can't predict all exception types -->
        <exclude name="AvoidCatchingGenericException"/>
    </rule>

    <!-- Configure ExcessiveImports with custom threshold for Vaadin views -->
    <rule ref="category/java/design.xml/ExcessiveImports">
        <properties>
            <property name="minimum" value="40"/>
        </properties>
    </rule>

    <!-- Configure complexity rules with custom thresholds -->
    <!--
    CyclomaticComplexity: Maximum 10 per method
    Based on industry best practices (Google, Amazon recommend 10-15, we use stricter 10)
    Cognitive complexity should also be <= 10 (checked by SonarLint)
    High complexity makes code harder to understand, test, and maintain.
    If a method exceeds this limit, consider refactoring: extract methods, simplify logic.
    
    Note: Framework lifecycle methods (onInit, onReady, etc.) may have higher complexity
    as they are framework callbacks. These are acceptable and checked by SonarLint separately.
    -->
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="classReportLevel" value="80"/>
            <property name="methodReportLevel" value="10"/>
        </properties>
    </rule>
    <rule ref="category/java/design.xml/NPathComplexity">
        <properties>
            <property name="reportLevel" value="250"/>
        </properties>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <!-- Exclude MissingSerialVersionUID - not needed for modern Java -->
        <!-- Serialization is handled by frameworks (Jmix, Spring) -->
        <exclude name="MissingSerialVersionUID"/>
        <!-- Exclude NonSerializableClass - Jmix/Vaadin views contain framework dependencies -->
        <!-- Views are not intended for serialization, they contain framework-managed beans -->
        <exclude name="NonSerializableClass"/>
    </rule>

    <!-- Performance -->
    <rule ref="category/java/performance.xml">
    </rule>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>
    <!-- No exclusions - all security rules are critical -->

    <!-- Documentation -->
    <rule ref="category/java/documentation.xml">
        <!-- Exclude CommentRequired - Javadoc is optional for modern Java -->
        <!-- Code should be self-documenting through clear naming and structure -->
        <exclude name="CommentRequired"/>
    </rule>

    <!-- Configure documentation rules with custom limits -->
    <rule ref="category/java/documentation.xml/CommentSize">
        <properties>
            <property name="maxLines" value="50"/>
            <property name="maxLineLength" value="100"/>
        </properties>
    </rule>

    <!-- Lombok compatibility: Exclude specific rules for Lombok-generated code -->
    <!-- Lombok generates getters, setters, equals, hashCode, toString, constructors -->
    <!-- These methods follow standard JavaBean conventions and are generated code -->
    <!-- Note: PMD doesn't support fine-grained exclusions per rule, so we rely on -->
    <!-- SonarLint and Checkstyle for Lombok-specific suppressions -->

</ruleset>
