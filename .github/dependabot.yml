version: 2
updates:
  # Gradle dependencies - single group for all updates
  - package-ecosystem: 'gradle'
    directory: '/'
    schedule:
      interval: 'monthly'
      day: 'monday'
      time: '09:00'
    open-pull-requests-limit: 1 # One PR for all updates
    reviewers:
      - 'sovavibe'
    labels:
      - 'dependencies'
      - 'gradle'
    commit-message:
      prefix: 'build'
      include: 'scope'
    # Single group for all updates (patch and minor)
    groups:
      gradle-updates:
        patterns:
          - '*'
        update-types:
          - 'minor'
          - 'patch'
    # Ignore major updates automatically (require manual review)
    ignore:
      - dependency-name: '*'
        update-types:
          - 'version-update:semver-major'

  # GitHub Actions - single group for all updates
  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'monthly'
      day: 'monday'
      time: '09:00'
    open-pull-requests-limit: 1 # One PR for all updates
    reviewers:
      - 'sovavibe'
    labels:
      - 'dependencies'
      - 'github-actions'
    commit-message:
      prefix: 'ci'
      include: 'scope'
    groups:
      github-actions-updates:
        patterns:
          - '*'
        update-types:
          - 'minor'
          - 'patch'

  # Docker - single group for all updates
  - package-ecosystem: 'docker'
    directory: '/'
    schedule:
      interval: 'monthly'
      day: 'monday'
      time: '09:00'
    open-pull-requests-limit: 1 # One PR for all updates
    reviewers:
      - 'sovavibe'
    labels:
      - 'dependencies'
      - 'docker'
    commit-message:
      prefix: 'build'
      include: 'scope'
    groups:
      docker-updates:
        patterns:
          - '*'
        update-types:
          - 'minor'
          - 'patch'
    # Ignore major updates automatically
    ignore:
      - dependency-name: '*'
        update-types:
          - 'version-update:semver-major'
